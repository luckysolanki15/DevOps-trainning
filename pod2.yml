kind: Pod                              
apiVersion: v1                     
metadata:                           
  name: testpod2
  annotations:
    description: creating first testpod     
spec:                                    
  containers:                      
    - name: c02                     
      image: ubuntu              
      command: ["/bin/bash", "-c", "while true; do echo Hello-Ajay; sleep 5 ; done"]
  restartPolicy: Never         # Defaults to Always

